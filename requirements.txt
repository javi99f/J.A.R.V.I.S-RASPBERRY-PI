sounddevice>=0.5,<1
PyQt6>=6.6,<7
google-genai>=1.75,<2
google-generativeai>=0.8,<1
websockets>=16.1,<17
requests>=2.31,<3
ddgs>=9,<10
psutil>=5.9,<8
Pillow>=10,<13
numpy>=1.26,<3
# Python 3.13 ARM64 has no compatible tflite-runtime wheel.  The Raspberry
# installer installs openWakeWord separately with --no-deps and uses ONNX.
# Keeping this marker also lets older Jarvis updaters install new requirements
# without trying (and failing) to resolve tflite-runtime again.
openwakeword==0.6.0; python_version < "3.13"
spotipy>=2.25,<3
